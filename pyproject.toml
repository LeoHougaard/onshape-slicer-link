[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "onshape-slicer-link"
version = "0.3.2"
description = "Local Onshape connection for unmodified OrcaSlicer and Bambu Studio"
requires-python = ">=3.11"
license = "AGPL-3.0-only"
dependencies = ["cryptography>=44,<47", "keyring>=25,<26", "fastapi>=0.115,<1", "uvicorn>=0.34,<1"]

[project.optional-dependencies]
server = [] # Retained for existing development commands; the local app includes the service.
dev = ["pytest>=8,<10", "httpx>=0.28,<1", "pyinstaller>=6.16,<7", "ruff>=0.11,<1"]
browser = ["playwright>=1.55,<2"]
desktop-test = ["pywinauto>=0.6.9,<1; sys_platform == 'win32'", "pillow>=11,<13"]
embedded-dev = ["httpx>=0.28,<1", "websockets>=15,<17", "playwright>=1.55,<2"]

[project.scripts]
onshape-slicer-link = "slicer_link.local:main"
onshape-slicer-link-server = "slicer_link.service:main"

[tool.setuptools.packages.find]
include = ["slicer_link*"]

[tool.setuptools.package-data]
slicer_link = ["web/*", "assets/*"]

[tool.pytest.ini_options]
testpaths = ["tests/stock"]

[tool.ruff]
target-version = "py311"
line-length = 110
